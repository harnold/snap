Snap can be run from any directory.  If you want to make it available
for all users, execute the following commands as root (or using sudo):

  install snap /usr/local/bin
  install -m 644 snap.1 /usr/local/man/man1
